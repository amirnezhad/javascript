function sum() {
    var res = porampt("Enter Number");
    var res2 = porampt("Enter Number2");
    var Num1 = parsInt(res);
    var Num2 = parsInt(res2);
    var sum = Num1 + Num2
    console.log(sum);
}